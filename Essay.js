//QUESTION ONE 
/*
forEach ==>  is used to loop through all elements in an array and perform an action on each element. 
It is useful when we want to do something with every element.
--------
for ..of ==>  is used to loop through the values of an array one by one. 
It is useful when we want more control over the loop, such as using break or continue.

*/

//QUESTION Two 
/*
Hoisting ===> js processes all the variable and function declarations
to the top of the scope before running the code.   
ex: console.log(x);
let x=9; 

TDZ ==> it is the period where let and const are known but can't be used until initialization.
in var it will hoisted then initialization as undefined
but in lat and const will be hoisted and will be in Temporal Dead zone

*/

//QUESTION Three
/*
== compares values only  9 == "9"  true  (type coercion)
=== compares values and types  9 === "9"  false

*/

//QUESTION Four
/*
try - catch is a way to handle errors 
we put the code that might cause an error in the try block
and If an error occurs it is caught and handled by the catch block  
it is important in async operations because it can handle errors from rejected Promises.

*/

//QUESTION Five
/*

Type coercion ==> it can automatically convert types
"5"+1 = 51
"5"-1 = 4

Type  conversion ==> it can manually convert types
String(123) Number("123")  Boolean(1)  

*/
